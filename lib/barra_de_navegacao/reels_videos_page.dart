import 'package:flutter/material.dart';

class ReelsVideosPage extends StatelessWidget {

  const ReelsVideosPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
      //  return Scaffold(
    //      appBar: AppBar(title: const Text('Loja'),),
    //      body: Container(),
    //  );
    return Container(
      color: Colors.yellow,
    );
  }
}